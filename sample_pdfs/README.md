# Sample PDFs

Drop sample bank statement PDFs in this folder. The parser harness will read all `*.pdf` files here.

Run:

```bash
python scripts/parse_sample_pdfs.py --input sample_pdfs --output outputs/parse_results.json
```
